language: java
jdk: oraclejdk8
addons:
  branches:
    - master
    - /^arko.*$/
    - arko/db-wip
script:
  # other script steps might be done before running the actual SonarQube analysis
  - sonar-scanner